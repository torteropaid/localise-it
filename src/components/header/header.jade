include ../modules/navbar/navbar
include ../modules/icon/icon
include ../spawn/spawn

#top
  +navbar-brand()
    a(href="./")
      img.logo(src="img/logo_lit.png", alt="logo")

  //-if login
  //-ul#mainMenu.nav.nav-tabs
    li(class="#{(menu==1) ? 'selected': ''}", data-tab="all")
      span.ion-btn.ion-btn-mainmenu All
    li(class="#{(menu==2) ? 'selected': ''}", data-tab="new")
      span.ion-btn.ion-btn-mainmenu New
    li(class="#{(menu==3) ? 'selected': ''}", data-tab="unchecked")
      span.ion-btn.ion-btn-mainmenu Unchecked
    li(class="#{(menu==4) ? 'selected': ''}", data-tab="search")
      span.ion-btn.ion-btn-mainmenu.glyphicon.glyphicon-search

  if loggedIn
    +navbar-right()
      //-if login
      ul#helper-menu
        li
          +icon({type: "open-menu glyphicon glyphicon-menu-hamburger"})
          //-.tooltip.fade.left.in Logout